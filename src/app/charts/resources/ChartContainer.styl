$buffer = 20px
.chart-container
    .btn-default
        right $buffer
        top $buffer
        position absolute
    .panel-default
        width 500px
        position absolute
        right $buffer + 88px
        top $buffer
